# 黄金 Dashboard 开发困难总结

> 记录 2026-08 开发过程中踩过的坑与沉淀的经验，按领域分类。

---

## 一、数据管线（fetch_data.py）

### 1.1 COT 解析：MICRO GOLD 误匹配 🔴
- **现象**：历史 COT 数据从 136 周变成 259 周，重复 123 条
- **根因**：`"GOLD - COMMODITY EXCHANGE" in name` 是**子串匹配**，`MICRO GOLD - COMMODITY EXCHANGE INC`（微型黄金期货）也包含该子串
- **教训**：旧代码用 `startswith('"GOLD - ...')` 恰好排除了它，重构为子串匹配时引入回归
- **解法**：改回 `name.startswith('GOLD - COMMODITY EXCHANGE')` + 按日期去重兜底

### 1.2 COT 解析：硬编码列索引脆弱
- **问题**：`parts[7]/parts[8]/parts[37]` 依赖 CFTC 格式固定，改格式会静默失败
- **解法**：解析表头行动态映射列名 → 索引，匹配失败回退历史固定位置
- **经验**：外部数据源解析优先"表头驱动"，永远不要硬编码列位置

### 1.3 COT 解析：越界预检过激
- **现象**：早期年度 zip 缺 changes 列时整行被拒（`len(parts) <= max(index.values())`）
- **解法**：删除整体预检，改为逐字段边界检查，缺失字段降级为 None
- **教训**：健壮性检查宁可逐项宽松，不要整体一刀切

### 1.4 GLD 持仓公式错误（数学正确 ≠ 语义正确）
- **现象**：`totalAssets / (prev_close * 10)` 数值碰巧等于盎司数（因为 NAV≈金价/10），注释却写"用 totalAssets/金价"——公式和注释对不上，且**结果从未写入 JSON**
- **解法**：改为 `份额 = AUM/NAV`、`盎司 = 份额 × 0.1`，用官方 navPrice，字段写入 JSON + 前端展示
- **教训**：计算结果必须落地到输出才有效；"碰巧对"的公式迟早出问题

### 1.5 yfinance 限流（YFRateLimitError / HTTP 403）
- **现象**：5 个 ticker 全部失败，持续 20+ 分钟仍限流；curl 直接访问也是 403
- **认知**：Yahoo 限流是 **IP 级**的，可能持续数小时到一天，不是等待几分钟能解决的
- **解法**：
  1. `Ticker.info` 单独 try，失败降级为仅成交数据（原实现整个函数返回 `{}`，连 flow5dPct 都丢）
  2. yfinance 全部失败时**从旧 dashboard.json 回填历史序列**（打印 ⚠ 警告），保证管线跑通
  3. 关键序列为空时 main() 友好退出，不再 `np.percentile` 空数组 IndexError
- **经验**：数据管线必须有"降级路径"，外部源不可用时用旧数据兜底优于崩溃

### 1.6 打印 None 崩溃
- **现象**：`print(f"...{holdings_oz / 1e6:.1f}M...")` 当 holdings_oz 为 None 时 TypeError
- **教训**：f-string 里的除法不做 None 防护必炸，先格式化字符串再打印

### 1.7 Windows 中文乱码
- **现象**：Python 输出中文在终端乱码，JSON 数据本身是好的
- **解法**：运行加 `PYTHONIOENCODING=utf-8`
- **经验**：Windows 下调试中文输出必须先设这个环境变量，否则会误判数据损坏

### 1.8 回填数据只有 365 条，MA200 预热不足 🕳️
- **现象**：图表里 MA200 线"越靠近现在越没有"——gold 历史仅 365 条（约 1.4 年），goldMA200 仅 166 条
- **根因**：yfinance 限流时 `_fallback_old` 回填的是旧 dashboard.json 的 history，而 history 只存了最近 365 天 → 回填后 MA 计算窗口（50/200 日）不足
- **解法**：前端按日期对齐（缺失填 null + spanGaps）保证不错位；数据层根治待办：回填应保留更长历史或多次累计
- **教训**：回填的"旧数据"质量取决于上次输出，历史截断会逐级传导到衍生指标

---

## 二、前端渲染验证（最大的坑区）

### 2.1 Edge headless --virtual-time-budget 不可靠 ⚠️⚠️
- **现象**：同样的命令时好时坏——一次 dump 出完整 DOM，下一次输出空白（没有卡片）
- **根因**：虚拟时钟**不等真实网络请求**。fetch 441KB JSON 未完成时 budget 耗尽，dump 的是初始 HTML
- **误判**：曾误以为页面渲染失败，实际是验证工具问题
- **解法**：改用 **puppeteer-core + 本机 Edge**（`executablePath` 指定 msedge.exe），`waitUntil: 'networkidle2'` + 真实等待 2-3 秒，从此稳定

### 2.2 countUp 动画在虚拟时间模式下只跑部分
- **现象**：Hero 数字显示中间值 $3,906（正确值 $4,401）
- **教训**：rAF 动画在 headless 虚拟时间下不可靠，验证时等待真实时间或直接关动画

### 2.3 正则误匹配 JS 源码 ❗
- **现象**：dump-dom 输出**包含 `<script>` 源码**，正则匹配到模板字符串字面量（`${c.label}`、`${cur != null ? ...}`），误判"渲染失败/字面量残留"
- **解法**：验证脚本只查 `</script>` 之后的 body，或用 puppeteer `page.evaluate` 直接读 DOM
- **经验**：写验证断言前先想清楚"我在匹配渲染结果还是源码"

### 2.4 函数漏参导致静默中断 🕳️
- **现象**：`renderHero(p)` 内部用了 `vol` 但只传了 `p` → ReferenceError → **无 pageerror 捕获到**，后续模块全部静默不渲染，只看到"页面少了东西"
- **解法**：每个渲染步骤包 try/catch，错误收集到 `window.__aerr`，页面渲染完成标记 `data-ready` 确认执行到底
- **经验**：前端"少了一块"先查 JS 异常，再查数据；重构时注意函数签名和调用点同步

### 2.5 headless 进程残留
- **现象**：连续跑 msedge headless，后续进程行为异常
- **解法**：`taskkill //F //IM msedge.exe` + 每次用独立 `--user-data-dir`

### 2.6 其他环境坑
- Git Bash 下 `/tmp` 路径不可用，临时文件放项目目录
- `node --check <(python ...)` 进程替换在 Windows 不可用
- 验证依赖（puppeteer-core）用后即删，不污染项目仓库

### 2.7 Chart.js 散点 tooltip 的 label 陷阱 ⚠️⚠️
- **现象**：C 视图开场图事件点 hover 只有时间没有事件名；个别点显示的甚至是别的事件（张冠李戴）
- **根因**：category 轴下散点 `{x: 日期字符串}` 的 tooltip `item.label` 由**数据集内索引**生成（`labels[k]`），与事件真实日期错位；而 `parsed.x` 反而是正确的日期索引——用 label 反查数据必然错乱（7 个点只有 1 个碰巧匹配）
- **解法**：散点数据直接携带事件引用 `_ev`，tooltip 从 `c.raw._ev` 读取，与 label/parsed 机制彻底解耦
- **教训**：混合图表里散点的 label 生成机制与直觉相反，不要依赖 label 反查数据；数据自带引用最稳

### 2.8 散点紧贴折线：命中列表多元素
- **现象**：点击事件点不跳转（onClick 静默 return）
- **根因**：nearest 命中列表按距离排序，散点紧贴金价线时**首个元素是折线点（ds=0）**而非散点（ds=3），`els[0].datasetIndex !== 3` 直接拦截
- **解法**：`els.find(a => a.datasetIndex === 3)` 在命中列表中查找目标系列，tooltip title 同理（`items.map(i => i.raw._ev).find(Boolean)`）
- **经验**：多系列贴合场景（散点画在线上）必须按 datasetIndex 过滤，不能假设第一个元素

### 2.9 CloakBrowser 验证图表交互（新工具）
- **用法沉淀**：
  - ESM 包：`.mjs` 脚本 + `import from 'file:///绝对路径/dist/index.js'`（NODE_PATH 对 ESM 无效）
  - tooltip 内容：`chart.tooltip.title` / `chart.tooltip.body[].lines` / `chart.tooltip.dataPoints`（直接读内部状态，比截图精确）
  - hover/click 模拟：`page.mouse.move(px, py)`，坐标 = `canvas.getBoundingClientRect()` + `chart.getDatasetMeta(i).data[k].x/y`
  - 元素真实解析值：`el.$context.parsed`（比像素反推可靠）
- **经验**：验证图表交互直接读 Chart 实例状态，先看数据再下结论；
  本次靠它区分了"绘制位置正确但 tooltip label 错乱"两个层面

---

## 三、视觉设计

### 3.1 第一版被否：换皮不破架构
- **用户反馈**："这也太丑了"——不是细节问题，是**架构**问题：仍是卡片墙从上往下堆
- **解法**：三视图架构（杂志叙事/终端分屏/时间线），让信息层级重新排序
- **经验**：用户说丑时，先怀疑布局范式而非配色

### 3.2 对比度不达标（WCAG）
- **现象**：`--text-3: #6b6354` 在炭黑底只有 **3.3:1**（AA 要求 4.5:1），小字看不清
- **解法**：颜色提亮（text-3 → #9c9382 = 6.5:1）+ 字号系统提升（最小 .56rem 9px → .8rem 12.8px）
- **经验**：深色主题至少准备三级文字令牌，并实测对比度；小字下限 12px（0.75rem）

### 3.3 重构时丢功能
- **现象**：三视图重构把 9 图表 tab（含时间范围选择）弄丢了，用户主动发现
- **教训**：重构前列出功能清单逐项核对；重构 ≠ 丢弃

### 3.4 同级视图一致性：对比度问题分两轮暴露
- **现象**：第一轮修完 A 视图对比度后，B 视图（最小 .56rem 小字 + #8a8a8a 次文字）与 C 视图（卡片 #14110c 与背景 #0d0b08 明度差仅 7/255，卡片"陷"在背景里）相继被反馈同样问题
- **解法**：建立统一视觉基线——最小字号 .74rem、文字三级令牌对齐、卡片与背景至少 2 档明度差；改一处时同步检查其他视图
- **经验**：用户说"参考 A 页面"= 项目内部一致性要求；视觉参数应全局基线化，而非逐视图自定义

---

## 四、方法论沉淀

1. **外部数据源四原则**：表头驱动解析、不硬编码索引、容错降级、失败回填旧数据
2. **验证三件套**：语法检查 → puppeteer 真实渲染 → 错误收集数组（`window.__xerr`）+ 完成标记（`data-ready`）
3. **每一步改动都要能验证**：改完立即跑验证，不要攒着最后一起测
4. **多视图架构**：CSS scoped 前缀隔离（.view-a/.view-b/.view-c）、Chart 实例统一管理（destroy 防泄漏）、localStorage 记忆偏好
5. **动效规范**：150-300ms、easeOut、prefers-reduced-motion 全局尊重、入口动画用 stagger
6. **代码即文档**：每个自定义阈值/权重注释理由；PLAN.md 先行再动手

---

## 五、快速排查清单

| 症状 | 优先检查 |
|------|---------|
| 页面少模块 | 浏览器 console → `window.__aerr` → 渲染函数 try/catch |
| 数据对不上 | 是否回填了旧数据（⚠ 警告）→ 限流？ |
| headless 验证空白 | 换 puppeteer 真实等待；勿信 virtual-time-budget |
| 中文乱码 | PYTHONIOENCODING=utf-8 |
| 数值"碰巧对" | 检查语义是否真正确、结果是否真落地 |
| 图表散点 hover 无内容 | tooltip label 机制错位 → 数据携带引用直接读（`c.raw._ev`） |
| 均线右端消失 | 序列长度不同（预热期）→ 按日期对齐而非索引对齐 |
| 点击图表不跳转 | 命中列表首个元素是其他系列 → `find` 目标 datasetIndex |
