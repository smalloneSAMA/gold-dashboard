# 黄金 Dashboard 三风格重构计划

> 目标：在同一页面实现 A/B/C 三种完全不同的视觉架构，顶栏一键自由切换，
> 偏好本地记忆。数据层（dashboard.json / fetch_data.py）零改动。

---

## 一、总体架构：单页 · 三视图

```
data/dashboard.json ──┐
                      ├── index.html
index.html 内部结构：  │
├── 共享层（三视图共用） │
│   ├── 数据加载 loadData()          （不变）
│   ├── 工具函数 fmt / 信号解析等
│   ├── 主题切换器 viewSwitcher      （新）
│   └── 顶部 Status Bar              （新，三视图共用）
│
├── 视图 A · Golden Ledger 金账本     （叙事杂志，先实现）
├── 视图 B · Terminal 终端           （Bloomberg 分屏，后实现）
└── 视图 C · Timeline 时间线         （叙事滚动，后实现）
```

### 切换机制设计

- 顶栏右侧三个切换按钮：`A 金账本 / B 终端 / C 时间线`（字母徽章 + 名称）
- 点击切换 → 隐藏当前视图容器 → 渲染目标视图 → 150ms fade 过渡
- `localStorage['gold_view']` 记忆偏好，下次打开直接进入上次视图
- 三套 CSS 用 scoped 前缀隔离：`.view-a .xxx`、`.view-b .xxx`、`.view-c .xxx`
- 三套渲染入口：`renderViewA()` / `renderViewB()` / `renderViewC()`，切换时销毁旧 Chart 实例

### 共享 Status Bar（三视图统一）

一行：品牌徽标 · 标题 · 综合信号徽章（多/空/中性）· 金价实时 · 情绪分 · 视图切换器 · 更新时间。
三视图都保留这一行，保证切换时用户不迷失。

---

## 二、视图 A · Golden Ledger 金账本（叙事杂志风）

### 概念
把仪表盘改造成"一份可读的黄金市场简报"：数字是主角，图表嵌进叙事流，
模块不对称错位，金色 hairline 分隔，像金融杂志跨页。

### 布局（非对称 12 列网格）

```
┌──────────────────────────────────────────────────────────┐
│ STATUS BAR（品牌 · 信号 · 金价 · 情绪 · 切换器）             │
├──────────────────────────────────────────────────────────┤
│ HERO 金价头条（整宽）                                      │
│   "COMEX 黄金现价" 小标 + 8rem 巨数字 $4,401 + 涨跌徽章     │
│   右侧 90 日 sparkline（无轴纯线）                        │
│   底部 hairline 下的当日摘要一行（波动率/金银比/背离 Z）      │
├───────────────┬───────────────┬─────────────────────────┤
│ 引擎① 实际利率   │ 引擎② 美元     │ 引擎③ 通胀预期            │
│ 宽1.2 · 大读数   │ 宽1.0 · 大读数  │ 宽0.8 · 大读数           │
│ +60日迷你走势    │ +60日迷你走势   │ +60日迷你走势            │
├──────────────────────────────────────────────────────────┤
│ UNDERTOW 暗流带（整宽，非卡片）                            │
│   COT净多百分位 · GLD资金流 · 央行购金 · 金银比             │
│   4 条横向渐变量规（细条 + 端点标签），像仪表刻度带          │
├───────────────────────┬──────────────────────────────────┤
│ 多空天平（左 40%）      │ 情绪水位 + 雷达（右 60%）          │
│   看多块/看空块对置     │ 全宽渐变潮汐带 + 指针              │
│   支点指针=综合方向     │ 雷达图小尺寸嵌右下                  │
├──────────────────────────────────────────────────────────┤
│ RISK QUADRANT 风险象限（四标的位置于罗盘方位）              │
│   离中心越远风险越高 · 每条辐条 = 风险分数刻度               │
├──────────────────────────────────────────────────────────┤
│ TIMELINE 走势时间轴（短→中→长 一条金线上三个节点）           │
│   节点大小=置信度 · 节点颜色=方向 · 下方因子列表              │
└──────────────────────────────────────────────────────────┘
   技术面 → 右上角按钮展开的侧栏抽屉（不在主视区）
```

### 视觉语言
| 项 | 设定 |
|----|------|
| 底色 | 暖炭黑 `#0b0a07`（带暖调，区别于旧版冷蓝黑） |
| 标题字体 | `Noto Serif SC` 600（杂志衬线） |
| 数字字体 | `Fira Code`（等宽） |
| 分隔 | 金色 hairline 0.5px `rgba(232,181,62,.18)` |
| 背景 | 淡网格 + 顶部金色辉光，模拟金库账本 |
| 动效 | Hero 数字滚动入场、天平倾斜、水位指针滑动（3 个高光时刻） |
| 图表 | 3 个主力（金价+均线 / 利率+美元 vs 金价 / 流动性），其余收进"更多"折叠 |

### 打破点（相对旧版）
- 等大卡片墙 → 1 个整宽 Hero 巨数字
- 信号双列列表 → 天平可视化
- 情绪小仪表 → 全宽水位带
- 风险表格 → 罗盘象限
- 展望三卡 → 横向时间轴
- 技术面九宫格 → 侧栏抽屉

---

## 三、视图 B · Terminal 终端（Bloomberg 分屏，后实现）

### 概念
极简高密度单屏终端：1440px+ 下四象限分屏（pane），几乎零留白，全部信息一屏可见。
每个 pane 带 1px 边框和等宽小字标题栏，像 Bloomberg / 彭博终端。

### 布局（四象限 + 状态行）

```
┌──────────────────────────────────────────────────────────┐
│ STATUS BAR（压缩版：信号·金价·切换器）                       │
├───────────────────────────────┬──────────────────────────┤
│ PANE① PRICE 金价+均线大图     │ PANE② MACRO 宏观引擎       │
│   （主图，占 2 行高）           │   实际利率/美元/通胀/VIX    │
│                               │   四行紧凑读数+迷你条       │
├───────────────────────────────┼──────────────────────────┤
│ PANE③ 资金流                   │ PANE④ 信号/风险            │
│   COT百分位 · GLD流 · 金银比    │   多空信号滚动行 + 风险条   │
│   情绪水位（窄）               │   走势预期一行              │
└───────────────────────────────┴──────────────────────────┘
   技术面数字行（底部一条，滚动显示）
```

### 视觉语言
| 项 | 设定 |
|----|------|
| 底色 | 纯黑 `#000` |
| 字体 | 全 Fira Code，10-12px |
| 配色 | 终端三色：绿 `#4af626` / 红 `#ff5f56` / 琥珀 `#ffb000` |
| 分隔 | 1px `#1a1a1a` pane 边框 |
| 动效 | 极少：数据闪烁（::after 扫描线）、光标呼吸 |
| 响应式 | 桌面四象限；<980px 降级为 tab 切换的 4 个 pane |

### 打破点
- 彻底放弃"卡片/圆角/阴影"——全直角、全边框、无阴影
- 零滚动（桌面一屏），信息密度最大化
- 无衬线标题、无品牌感，纯工具

---

## 四、视图 C · Timeline 时间线（叙事滚动，后实现）

### 概念
页面 = 一根可滚动的"黄金一年之旅"：金价一年走势作为纵向主轴贯穿全页，
驱动事件自动提取并标注在曲线上，每个章节讲一个故事。

### 布局

```
┌──────────────────────────────────────────────────────────┐
│ STATUS BAR                                                │
├──────────────────────────────────────────────────────────┤
│ 开场：一年金价总览（大图） + "这一年发生了什么"摘要           │
├──────────────────────────────────────────────────────────┤
│ 时间轴主体：中央金线 = 金价曲线（分段展开）                 │
│   事件节点自动提取：                                      │
│   · 利率 20 日动量极值点（拐点）                           │
│   · VIX > 30 的时段（避险带，金色底纹）                    │
│   · MA50/200 金叉死叉点                                   │
│   · 金银比 >80 / <60 时段                                  │
│   每个节点：日期 + 当时因子读数 + 一句话解读                │
│   左右交错排版（左文右图/左图右文，不对称）                  │
├──────────────────────────────────────────────────────────┤
│ 结尾：当前状态一行带过（信号/情绪/风险徽章横排）             │
└──────────────────────────────────────────────────────────┘
```

### 视觉语言
| 项 | 设定 |
|----|------|
| 底色 | 深褐黑 `#0d0b08`（档案/羊皮纸感） |
| 标题 | Noto Serif SC |
| 曲线 | 金色主线，事件节点金色发光圆点 |
| 章节 | 大段留白 + 章节序号（01/02/03 大字衬线） |
| 动效 | 滚动渐入（IntersectionObserver，stagger 150ms） |

### 打破点
- 完全抛弃"仪表盘"形态：没有网格、没有卡片、没有仪表
- 时间顺序主导，而不是指标分类主导
- 数据点变成"叙事事件"，用户读的是故事不是面板

---

## 五、实施顺序

| 步骤 | 内容 | 验收 |
|------|------|------|
| 1 | 重构 index.html 骨架：Status Bar + 三视图容器 + 切换器 + localStorage | 切换器可切换空视图 |
| 2 | **视图 A 完整实现**（本轮） | 无头渲染：Hero/引擎/暗流/天平/水位/象限/时间轴全部出现 |
| 3 | 视图 B 完整实现 | 四象限单屏渲染 |
| 4 | 视图 C 完整实现（事件自动提取逻辑） | 时间轴事件节点生成 |
| 5 | 三视图切换回归测试 + 移动端适配 | 三视图均正常 |

## 六、技术要点与风险

- **单文件体积**：三套 CSS+JS 预计 70-90KB，静态部署仍无压力；若超限可拆 css 文件
- **Chart 实例管理**：切换视图时统一 destroy 旧实例，防止内存泄漏/画布重叠
- **事件提取（视图 C）**：纯前端从 history 计算（20日动量极值、MA 交叉、VIX 阈值），无需改数据层
- **无障碍**：三视图共用键盘焦点策略；切换器 aria-pressed；prefers-reduced-motion 全局尊重
- **字体加载**：Noto Serif SC / Fira Code 走 Google Fonts，失败 fallback 衬线/等宽系统字体
- **移动端**：A 视图流式单列；B 视图 tab 化；C 视图时间轴单列——三视图分别处理断点
